import template from './{{ camelCase name }}.html';
import controller from './{{ camelCase name }}.controller';

let {{ camelCase name }}Component = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default {{ camelCase name }}Component;