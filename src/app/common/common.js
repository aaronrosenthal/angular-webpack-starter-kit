import angular from 'angular';

/** Import common component modules here */

let commonModule = angular.module('app.common', [
    /** Inject common component modules here */
]).name;

export default commonModule;